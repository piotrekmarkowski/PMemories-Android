<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Only needed for the debug MediaStore-seed shortcut used to verify
         the Studio export pipeline without driving the system Photo Picker
         UI. The real picker flow (PickVisualMedia) needs no permission. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Only needed on API 28 and below to save exports into the public
         Movies folder — API 29+ uses scoped-storage MediaStore inserts,
         which need no permission. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- Etap 4 (14.09.2026) — Firebase Auth/Firestore (logowanie Google,
         Ranking) potrzebują sieci. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Etap 7 (15.09.2026) — jednorazowa lokalizacja dla "Add peak from my
         location" (`PeakSearchProvider`, ten sam wzorzec co iOS
         `PeakDetector`), NIE ciągłe śledzenie w tle — appka świadomie nigdy
         nie ma background location, patrz `Brand.md`. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".PMemoriesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PMemories">
        <!-- Etap 7 — Google Maps SDK key, patrz komentarz przy `mapsApiKey`
             w app/build.gradle.kts. Placeholder pusty do czasu założenia
             klucza w Google Cloud Console. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PMemories">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
